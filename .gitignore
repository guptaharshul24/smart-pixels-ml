# Ignore trained models
trained_models/

# Ignore Python bytecode
__pycache__/
*.py[cod]
*.so

# Ignore Jupyter notebook checkpoints
.ipynb_checkpoints/

# Ignore logs and debug output
*.log
*.tmp

# Ignore data and cache files
*.npy
*.npz
*.pkl
*.h5
*.hdf5
*.ckpt
*.pt
*.onnx

# Ignore editor/system files
*.swp
*.swo
*.DS_Store
*.vscode/
*.idea/

# Ignore virtual environments
venv/
.env/
.envrc
.venv/
*.csv

# Ignore run logs
runLOG*.txt
*.out
*.err

# Ignore generated plots/animations -- reproducible from the .py scripts that make them
*.png
*.gif

# Jennet's dataset3sr/residuals.json (scp'd from her personal nobackup dir) and our
# verbatim/reformatted copy of it -- not ours to redistribute without checking with her
# first. residuals_frontend.json (our own generated data) is NOT covered by this.
ADC_effect_training/plotting/residual_comparison/dataset3sr_residuals_raw.json
ADC_effect_training/plotting/residual_comparison/residuals_pixelav_3sr.json

# Superseded QConv2D fix attempts, kept locally for reference only. These are the
# things tried BEFORE the real cause (QKeras needing Keras 2 -- see
# ADC_effect_training/README.md) was found: the warm-start script and its eval, the
# reverted losses/loss.py dead-zone patch, and the eval outputs of the stuck runs.
# None of it is part of the working pipeline.
ADC_effect_training/wrong_qconv_fixes/
